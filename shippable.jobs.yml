jobs:
  - name: tp-manifest1
    type: manifest
    steps:
      - GET: tp-img2
      - GET: triggertest1
     
  - name: tp-testdeploy1
    type: ecsDeploy
    steps:
      - GET: tptest-tpcluster
      - GET: tp-manifest1
    
  #- name: repo-dcldeploy
   # type: dclDeploy
    #steps:
     # - GET: repo-manifest
      #- GET: repo-dclcluster
      #- GET: repo-replica1
    
  #- name: repo-ecsdeploytodcldeploy
   # type: dclDeploy
    #steps:
     # - GET: repo-ecsdeploy
      #- GET: repo-dclcluster
      
#  - name: multimanifest-dcldeploy
 #   type: dclDeploy
  #  steps:
   #   - GET: repo-manifest
    #  - GET: repo-manifest2
     # - GET: repo-dclcluster
      
